version: '3'

services:
  golf7-sale:
    image: $DOCKER_IMAGE
    container_name: $APP_NAME
    restart: unless-stopped
    ports:
      - '2222:80'

networks:
  default:
    external:
      name: golf-network